---
categories: Solidworks-macro
title:  SOLIDWORKS Macro - Create Holes from Hole Wizard
permalink: /solidworks-macros/create-hole-from-hole-wizard/
tags:   [Solidworks Macro]
layout: docs
subnav: solidworks-macros
---

<div class="fluent-knowledge-base" markdown="1">

Objective of this article is to learn *how to create **Holes using 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="Hole Wizard" %}** through SOLIDWORKS VBA Macros* in SOLIDWORKS.

We create **Holes** in 3 steps in general.

1. Select **a 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketch_members.html" text="Sketch" %}** from we take the Holes location.
2. Loop through each 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketchPoint_members.html" text="**Sketch point**" %}.
3. Create **Holes using 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="Hole Wizard" %}** feature using **`HoleWizard`** method.

This method is ***not updated*** method, so ***use this method*** if you want to quickly create a new **Holes using Hole Wizard**.

## Steps To Create Holes using Hole Wizard

We use following steps to create **Holes using 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="Hole Wizard" %}** while writing macro

1. Ask user to select **a 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketch_members.html" text="Sketch" %}** from Feature tree.
2. From this **sketch** we get 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketchPoint_members.html" text="**Sketch point**" %} for *Holes position*.
3. Loop through each 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketchPoint_members.html" text="**Sketch point**" %}.
4. Create **Holes using 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="Hole Wizard" %}** method from 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.IFeatureManager_members.html" text="**SOLIDWORKS Feature Manager**" %}.

## Results We Can Get

After running our we successfully create **Hole feature** as a result.

Below image shows the result we get.

{% include image-zoom.html
  src="/assets/Solidworks_Images/hole-wizard/hole-wizard-final-result.png"
  alt="Hole wizard final result"
  caption="Figure: Hole wizard final result"
%}

{% capture body %}
To get the correct result please follow the steps correctly.
{% endcapture %}

{% include callout-note.html
  title="Correct selection"
  content=body
%}

## Video of Code on YouTube

Please see below 🎬 video on **how to create Hole feature** from SOLIDWORKS VBA Macros.

{% include video-block.html 
  video_id="1wEli-v29AQ"
  caption="How to Create Hole Feature using Hole Wizard"
%}

{% capture note_body %}

Please note that there are **no explanations** in the video.  
**Explanation** of each step and why we write code this way is provided in this post.

{% endcapture %}

{% include callout-caution.html
  title="Important"
  content=note_body
%}

---

{% include adsense/adsense-horizontal-ads.html %}

---



## Code Sample

Below is the `code` for creating *Hole Feature* feature in **VBA** is given.

{% capture code_content %}

```vb
Option Explicit

' Solidworks application variable
Dim swApp As SldWorks.SldWorks

' Solidworks document variable
Dim swDoc As SldWorks.ModelDoc2

' Solidworks Feature variable
Dim swFeature As SldWorks.Feature

' Solidworks Sketch variable
Dim swSketch As SldWorks.Sketch

' Solidworks Selection Manager variable
Dim swSelMgr As SldWorks.SelectionMgr

' Sketch point array
Dim vSketchPointArray As Variant

' Solidworks Sketch Point variable
Dim sketchPoint As SldWorks.sketchPoint

' Solidworks Hole Feature
Dim swHoleFeature As SldWorks.Feature

' Sketch Point inside point array
Dim vpoint As Variant

' Create Hole from Hole wizard program
Sub main()
    
  ' Set Solidworks application variable to current application
  Set swApp = Application.SldWorks
    
  ' Check if Solidworks is opened or not
  If swApp Is Nothing Then
    MsgBox ("Solidworks is not opened")
    Exit Sub
  End If
  
  ' Set Solidworks document variable to open document
  Set swDoc = swApp.ActiveDoc
  
  ' Check if Solidworks document is opened or not
  If swDoc Is Nothing Then
    MsgBox ("Solidworks document is not opened. Please open a document.")
    Exit Sub
  End If
  
  ' Set Solidworks Selection Manager variable
  Set swSelMgr = swDoc.SelectionManager
    
  ' Message to show user
  Dim messageToUser As String

  ' Update Messages
  messageToUser = "Please select Sketch from Feature tree for Hole location."

  ' Show message to user
  MsgBox messageToUser
  
  ' Variable for Solidworks Entity
  Dim swObject As SldWorks.Entity
    
  ' Loop until we complete our selection
  While swObject Is Nothing
      
    ' Local integer for loop
    Dim i As Integer
    
    ' Loop until we select
    For i = 1 To swSelMgr.GetSelectedObjectCount2(-1)
        
      ' If the profile sketch is selected
      If swSelMgr.GetSelectedObjectType3(i, -1) = swSelectType_e.swSelSKETCHES Then

        ' Set the Solidworks Entity object to profile sketch
        Set swObject = swSelMgr.GetSelectedObject6(i, -1)
      
      ' If the selected Sketch is externaly
      ElseIf swSelMgr.GetSelectedObjectType3(i, -1) = swSelectType_e.swSelEXTSKETCHPOINTS Then
          
        ' Inform user to select sketch from Tree
        MsgBox "Please select Sketch from Feature Tree."
        
        ' Clear selection
        swDoc.ClearSelection2 True
      End If
    Next
    
    DoEvents
  Wend
  
  ' Get and Set Solidworks Feature variable of Sketch2
  Set swFeature = swObject
  
  ' Get and Set Solidworks Sketch variable of Sketch2
  Set swSketch = swFeature.GetSpecificFeature2
      
  ' Get all sketch points inside sketch2
  vSketchPointArray = swSketch.GetSketchPoints2
  
  '-----------------------Unit Conversion Factors----------------------------------------
  
  ' Local variables used as Conversion Factors
  Dim LengthConversionFactor As Double
  Dim AngleConversionFactor As Double
  
  ' Use a Select Case, to get the length of active Unit and set the different factors
  Select Case swDoc.GetUnits(0)       ' GetUnits function gives us, active unit
    
    Case swMETER    ' If length is in Meter
      LengthConversionFactor = 1
      AngleConversionFactor = 1
    
    Case swMM       ' If length is in MM
      LengthConversionFactor = 1 / 1000
      AngleConversionFactor = 1 * 0.01745329
    
    Case swCM       ' If length is in CM
      LengthConversionFactor = 1 / 100
      AngleConversionFactor = 1 * 0.01745329
    
    Case swINCHES   ' If length is in INCHES
      LengthConversionFactor = 1 * 0.0254
      AngleConversionFactor = 1 * 0.01745329
    
    Case swFEET     ' If length is in FEET
      LengthConversionFactor = 1 * (0.0254 * 12)
      AngleConversionFactor = 1 * 0.01745329
    
    Case swFEETINCHES     ' If length is in FEET & INCHES
      LengthConversionFactor = 1 * 0.0254  ' For length we use sama as Inch
      AngleConversionFactor = 1 * 0.01745329
    
    Case swANGSTROM        ' If length is in ANGSTROM
      LengthConversionFactor = 1 / 10000000000#
      AngleConversionFactor = 1 * 0.01745329
    
    Case swNANOMETER       ' If length is in NANOMETER
      LengthConversionFactor = 1 / 1000000000
      AngleConversionFactor = 1 * 0.01745329
    
    Case swMICRON       ' If length is in MICRON
      LengthConversionFactor = 1 / 1000000
      AngleConversionFactor = 1 * 0.01745329
  End Select
  
  '----------------------------------------------------------------

  
  ' Loop through each point
  For Each vpoint In vSketchPointArray
    
    ' Set Solidworks Sketch point to current point in loop
    Set sketchPoint = vpoint
    
    ' Select current sketch point
    sketchPoint.Select True
    
    ' Create Counter Bore Hole at this point
    Set swHoleFeature = swDoc.FeatureManager.HoleWizard(swWzdCounterBore, swStandardISO, swStandardISOHexCapScrew, "M6", swEndCondBlind, 5 * LengthConversionFactor, 6 * LengthConversionFactor, 10 * LengthConversionFactor, 5 * LengthConversionFactor, 0, 1 * LengthConversionFactor, 2 * LengthConversionFactor, 0, 0, 0, 0, 0, 0, 0)
  Next
  
  ' Check if Holes from Hole Wizard creates or not
  If swHoleFeature Is Nothing Then
    MsgBox ("Failed to create Holes from Hole Wizard.")
    Exit Sub
  End If
  
  ' View zoom to fit
  swDoc.ViewZoomtofit2
  
  ' Clear all selection
  swDoc.ClearSelection2 True
  
End Sub
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
## Prerequisite

There are some *prerequisite* for this article.

We are **not** creating sketches from code but we use **existing sketch** to *create **Holes using 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="Hole Wizard" %}*** feature as shown in below picture.

{% include image-zoom.html
  src="/assets/Solidworks_Images/hole-wizard/prerequisite.png"
  alt="Prerequisite sketch and extrude feature for hole wizard"
  caption="Figure: Prerequisite sketch and extrude feature for hole wizard"
%}

As shown in above image, there are *1 sketches* and *1 Extrude feature* in our part.

- `Extrude Feature` : This is our **Extrude part** for 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="**Hole Wizard**" %} feature.
- `Sketch2` : This  👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketch_members.html" text="**Sketch**" %} contains  👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketchPoint_members.html" text="**Sketch point**" %}. which work as location for **Hole Feature**.

If you want to create `Sketch2` programmatically then please refer to below article.

- For **creating a Sketch Point** 👉 read {% include link-new-tab.html href="/solidworks-macros/create-point" text="**SOLIDWORKS Macros - Create a Point**" %} article.


If you want to create **Extrude** feature programmatically then please refer to below article.

* 👉 {% include link-new-tab.html href="/solidworks-macros/create-extrude-feature" text="**SOLIDWORKS Macros - Create Extrude/Boss**" %}

Also, we will apply checks in this article, so the code we write should be error free most of the time.

## Steps To Follow

To create **Holes using 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="Hole Wizard" %}**, there are following steps:

1. *Creating Global Variables*
2. *Initializing required variables*
3. *Ask user to select Sketch segment*
4. *Get all Sketch Points from selected Sketch*
5. *Get unit Conversion factors*
6. *Create Hole feature at each Sketch Point*
7. *Final work*

Now let us walk through **each step** as given above, and **understand** every line.

I also give some ***links*** (see icon 👉 ) so that you can go through them if there are anything I explained in previous articles.

### Creating Global Variables

{% capture code_content %}

```vb
Option Explicit
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
This line forces us to define every variable we are going to use. 

For more information please visit 👉 {% include link-new-tab.html href="/solidworks-macros/open-new-document" text="**SOLIDWORKS Macros - Open new Part document**" %} post.

We create following variables.

* **Variable for Solidworks application**

{% capture code_content %}

```vb
' Variable for Solidworks application
Dim swApp As SldWorks.SldWorks
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we create a variable which we named as `swApp` and the type of this `swApp` variable is `SldWorks.SldWorks`.

To see methods and properties related to `SldWorks.SldWorks` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISldWorks_members.html" text="**this page of SOLIDWORKS API Help**" %}.

* **Variable for Solidworks document**

{% capture code_content %}

```vb
' Variable for Solidworks document
Dim swDoc As SldWorks.ModelDoc2
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we create a variable which we named as `swDoc` and the type of this `swDoc` variable is `SldWorks.ModelDoc2`.

To see methods and properties related to `SldWorks.ModelDoc2` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.IModelDoc2_members.html" text="**this page of SOLIDWORKS API Help**" %}.

* **Variable for Solidworks feature**

{% capture code_content %}

```vb
' Variable for Solidworks feature
Dim swFeature As SldWorks.Feature
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we Create a variable which we named as `swFeature` and the type of this `swFeature` variable is `SldWorks.Feature`.

We create variable `swFeature` for **SOLIDWORKS Feature**.

To see methods and properties related to `Feature` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.IFeature_members.html" text="**this page of SOLIDWORKS API Help**" %}.

* **Variable for Solidworks Sketch**

{% capture code_content %}

```vb
' Solidworks Sketch variable
Dim swSketch As SldWorks.Sketch
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we create a variable which we named as `swSketch` and the type of this `swSketch` variable is `SldWorks.Sketch`.

To see methods and properties related to `SldWorks.Sketch` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketch_members.htm" text="**this page of SOLIDWORKS API Help**" %}.

* **Variable for Solidworks Selection Manager**

{% capture code_content %}

```vb
' Variable for Solidworks Selection Manager
Dim swSelMgr As SldWorks.SelectionMgr
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we create a variable which we named as `swSelMgr` and the type of this `swSelMgr` variable is `SldWorks.SelectionMgr`.

To see methods and properties related to `SldWorks.SelectionMgr` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISelectionMgr_members.html" text="**this page of SOLIDWORKS API Help**" %}.

* **Variable for Sketch point array**

{% capture code_content %}

```vb
' Sketch point array
Dim vSketchPointArray As Variant
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we create a variable named `vSketchPointArray` as `Variant` object type.

We create variable `vSketchPointArray` for storing all the sketch points we get from selected Sketch.

* **Variable for Solidworks Sketch Point**

{% capture code_content %}

```vb
' Solidworks Sketch Point variable
Dim sketchPoint As SldWorks.sketchPoint
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we create a variable which we named as `sketchPoint` and the type of this `sketchPoint` variable is `SldWorks.sketchPoint`.

To see methods and properties related to `SldWorks.sketchPoint` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketchPoint_members.html" text="**this page of SOLIDWORKS API Help**" %}.

* **Variable for Solidworks Hole Feature**

{% capture code_content %}

```vb
' Solidworks Hole Feature
Dim swHoleFeature As SldWorks.Feature
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we create a variable which we named as `swHoleFeature` and the type of this `swHoleFeature` variable is `SldWorks.Feature`.

We create variable `swFeature` for **SOLIDWORKS Hole Feature**.

To see methods and properties related to `Feature` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.IFeature_members.html" text="**this page of SOLIDWORKS API Help**" %}.

* **Variable for Sketch point inside array**

{% capture code_content %}

```vb
' Sketch Point inside point array
Dim vpoint As Variant
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we create a variable named `vpoint` as `Variant` object type.

We create variable `vpoint` for each point inside `vSketchPointArray`.

***These all are our global variables.***

They are **SOLIDWORKS API Objects**.

So basically I group all the **SOLIDWORKS API Objects** in one place.

{% capture code_content %}

```vb
' Create Hole from Hole wizard program
Sub main()

End Sub
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
Next is our `Sub` procedure which has name of `main`. 

This procedure hold all the *statements (instructions)* we give to computer.

To know more about **Sub Procedure** you can check 👉 {% include link-new-tab.html href="/vba/sub-and-function-procedure/" text="**VBA Sub and Function Procedures**" %} article of this website.

### Initializing Required Variables

Inside this procedure we first initialize required variables as given below.

* *Set SOLIDWORKS variable to SOLIDWORKS application*

{% capture code_content %}

```vb
' Set SOLIDWORKS variable to SOLIDWORKS application
Set swApp = Application.SldWorks
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we set the value of our SOLIDWORKS variable `swApp`; which we define earlier; to SOLIDWORKS application.

{% capture code_content %}

```vb
' Check if SOLIDWORKS is opened or not
If swApp Is Nothing Then
  MsgBox ("SOLIDWORKS is not opened")
  Exit Sub
End If
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we use an 👉 {% include link-new-tab.html href="/vba/if-then-structure-select-case/" text="**IF statement**" %} to check if SOLIDWORKS application variable is *successfully assigned* to current SOLIDWORKS application.

* *Set SOLIDWORKS document variable to opened part document*

{% capture code_content %}

```vb
' Set SOLIDWORKS document variable to opened part document
Set swDoc = swApp.ActiveDoc
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we set SOLIDWORKS document `swDoc` variable to currently open part document.

{% capture code_content %}

```vb
' Check if SOLIDWORKS document is opened or not
If swDoc Is Nothing Then
  MsgBox ("SOLIDWORKS document is not opened. Please open a document.")
  Exit Sub
End If
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we use an 👉 {% include link-new-tab.html href="/vba/if-then-structure-select-case/" text="**IF statement**" %} to check if SOLIDWORKS document `swDoc` is opened.

If SOLIDWORKS document is not opened then code execute inside the code and inform the user by a 👉 {% include link-new-tab.html href="/vba/msgBox-function/" text="**Message Window**" %}.

* *Set SOLIDWORKS Selection Manager variable*

{% capture code_content %}

```vb
' Set SOLIDWORKS Selection Manager variable
Set swSelMgr = swDoc.SelectionManager
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line, we set SOLIDWORKS Selection Manager`swSelMgr` variable to current document's Selection Manager.


---

{% include adsense/adsense-horizontal-ads.html %}

---### Ask user to select Sketch

Now we will ask user to select **a Sketch** for 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="**Hole Wizard**" %}

Please follow steps given below.

* *Show Message to user*

{% capture code_content %}

```vb
' Message to show 
Dim messageToUser As String
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code we create a variable named `messageToUser` of `String` type.

This variable holds the message we want to show before selection.

{% capture code_content %}

```vb
' Update Message
messageToUser = "Please select Sketch from Feature tree for Hole location."
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we set the value of `messageToUser` update to `"Please select Sketch from Feature tree for Hole location."`

{% capture code_content %}

```vb
' Show message to user
MsgBox messageToUser
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we show the message to user.

Below image show the message to the user.

{% include image-zoom.html
  src="/assets/Solidworks_Images/hole-wizard/ask-user-to-select-sketch.png"
  alt="Message asking user to select sketch"
  caption="Figure: Message asking user to select sketch"
%}

* Complete the selection

{% capture code_content %}

```vb
' Variable for Solidworks Entity
Dim swObject As SldWorks.Entity
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In this line, we Create a variable which we named as `swObject` and the type of this `swObject` variable is `SldWorks.Entity`.

We create variable `swObject` for **SOLIDWORKS Entities** (*Selected Sketch*) we ask use to select.

To see methods and properties related to `SldWorks.Entity` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.IEntity_members.html" text="**this page of SOLIDWORKS API Help**" %}.

{% capture code_content %}

```vb
' Loop until we complete our selection
While swObject Is Nothing
    
Wend
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we create another `While` loop.

This loop will continue until we select the our 👉{% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketch_members.html" text="**Sketch**" %}.

For more details about `While` loop, please see 👉 {% include link-new-tab.html href="/vba/looping/" text="**VBA Looping**" %} article from this website.

{% capture code_content %}

```vb
' Local integer for loop
Dim i As Integer
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we create a local integer name `i` as a counter.

{% capture code_content %}

```vb
' Loop until we select
For i = 1 To swSelMgr.GetSelectedObjectCount2(-1)

Next
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we create a `For` loop.

This code loops from `i = 1` to number of `objects` we select.

{% capture code_content %}

```vb
' If the sketch is selected
If swSelMgr.GetSelectedObjectType3(i, -1) = swSelectType_e.swSelSKETCHES Then

  ' Set the Solidworks Entity object to sketch
  Set swObject = swSelMgr.GetSelectedObject6(i, -1)

' If the selected Sketch is externaly
ElseIf swSelMgr.GetSelectedObjectType3(i, -1) = swSelectType_e.swSelEXTSKETCHPOINTS Then

  ' Inform user to select sketch from Tree
  MsgBox "Please select Sketch from Feature Tree."

  ' Clear selection
  swDoc.ClearSelection2 True
End If
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we use  👉 {% include link-new-tab.html href="/vba/if-then-structure-select-case/" text="**IF/Else statement**" %}  with conditions.

The  👉 {% include link-new-tab.html href="/vba/if-then-structure-select-case/" text="**IF statement**" %}  is `True` when selected `object` is type of *SOLIDWORKS Sketch*.

Similarly,  👉 {% include link-new-tab.html href="/vba/if-then-structure-select-case/" text="**Else statement**" %}  is True when select `object` is type of *SOLIDWORKS External Sketch Point*.

{% capture body %}
When we select the sketch from Model view, then selected object is *SOLIDWORKS External Sketch Point*.

When we select the sketch from Feature Tree, then select object is *SOLIDWORKS Sketch*.
{% endcapture %}

{% include callout-note.html
  title="Sketch selection source"
  content=body
%}

When  👉 {% include link-new-tab.html href="/vba/if-then-structure-select-case/" text="**IF statement**" %} is True we execute code given below.

{% capture code_content %}

```vb
' Set the Solidworks Entity object to sketch
Set swObject = swSelMgr.GetSelectedObject6(i, -1)
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line, we set the SOLIDWORKS Entities object `swObject` value by `GetSelectedObject6()` method of SOLIDWORKS Selection Manager.

Similarly, when  👉 {% include link-new-tab.html href="/vba/if-then-structure-select-case/" text="**Else statement**" %} is True we execute code given below.

{% capture code_content %}

```vb
' Inform user to select sketch from Tree
MsgBox "Please select Sketch from Feature Tree."

' Clear selection
swDoc.ClearSelection2 True
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
1. We show message to user to select 👉{% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketch_members.html" text="**Sketch**" %} from *Feature Tree*.
2. After that we clear our selection.

{% capture body %}
We do this because if selected object type is *SOLIDWORKS External Sketch Point* then we can not assign this object as SOLIDWORKS Entity inside array.
{% endcapture %}

{% include callout-note.html
  title="Why selection is cleared"
  content=body
%}

After `For` loop, we have a call for **`DoEvents`** function.

This function repeats the `While` loop until we select the 👉{% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketch_members.html" text="**Sketch**" %} for 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="**Hole Wizard**" %}.

### Get all Sketch Points from selected Sketch

Now we need to get the all 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketchPoint_members.html" text="**Sketch point**" %} from previously selected 👉{% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketch_members.html" text="**Sketch**" %} .

We do this by below code of lines.

{% capture code_content %}

```vb
' Get and Set Solidworks Feature variable of Sketch2
Set swFeature = swObject
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code we set the value of **SOLIDWORKS Feature** variable `swFeature` to `swObject`.

{% capture code_content %}

```vb
' Get and Set Solidworks Sketch variable of Sketch2
Set swSketch = swFeature.GetSpecificFeature2
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code we set the value of **SOLIDWORKS Sketch** variable `swSketch` by `GetSpecificFeature2` method from **SOLIDWORKS Feature** variable.

{% capture code_content %}

```vb
' Get all sketch points inside sketch2
vSketchPointArray = swSketch.GetSketchPoints2
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code we get all 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.ISketchPoint_members.html" text="**Sketch point**" %} by `GetSketchPoints2` from **SOLIDWORKS Sketch** variable `swSketch` and store them in `vSketchPointArray` variable.

### Get unit Conversion factors

Now we need to get unit Conversion factors as shown in below code.

{% capture code_content %}

```vb
'-----------------------Unit Conversion Factors----------------------------------------
  
' Local variables used as Conversion Factors
Dim LengthConversionFactor As Double
Dim AngleConversionFactor As Double

' Use a Select Case, to get the length of active Unit and set the different factors
Select Case swDoc.GetUnits(0)       ' GetUnits function gives us, active unit

Case swMETER    ' If length is in Meter
  LengthConversionFactor = 1
  AngleConversionFactor = 1

Case swMM       ' If length is in MM
  LengthConversionFactor = 1 / 1000
  AngleConversionFactor = 1 * 0.01745329

Case swCM       ' If length is in CM
  LengthConversionFactor = 1 / 100
  AngleConversionFactor = 1 * 0.01745329

Case swINCHES   ' If length is in INCHES
  LengthConversionFactor = 1 * 0.0254
  AngleConversionFactor = 1 * 0.01745329

Case swFEET     ' If length is in FEET
  LengthConversionFactor = 1 * (0.0254 * 12)
  AngleConversionFactor = 1 * 0.01745329

Case swFEETINCHES     ' If length is in FEET & INCHES
  LengthConversionFactor = 1 * 0.0254  ' For length we use sama as Inch
  AngleConversionFactor = 1 * 0.01745329

Case swANGSTROM        ' If length is in ANGSTROM
  LengthConversionFactor = 1 / 10000000000#
  AngleConversionFactor = 1 * 0.01745329

Case swNANOMETER       ' If length is in NANOMETER
  LengthConversionFactor = 1 / 1000000000
  AngleConversionFactor = 1 * 0.01745329

Case swMICRON       ' If length is in MICRON
  LengthConversionFactor = 1 / 1000000
  AngleConversionFactor = 1 * 0.01745329
End Select

'----------------------------------------------------------------
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
I have already explained about this in previous 👉 {% include link-new-tab.html href="/solidworks-macros/unit-correction/" text="**Solidworks Macro - Fix Unit Issue**" %} article in this website.

Please visit 👉 {% include link-new-tab.html href="/solidworks-macros/unit-correction/" text="**Solidworks Macro - Fix Unit Issue**" %} article for more details.

### Create Hole feature 

Now we create Hole feature at every Sketch Point.

* *Loop through every Sketch Point*

{% capture code_content %}

```vb
' Loop through each point
For Each vpoint In vSketchPointArray

Next
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code we create a 👉 {% include link-new-tab.html href="/vba/looping/" text="**For Each**" %} loop.

* *Select current Sketch Point*

{% capture code_content %}

```vb
' Set Solidworks Sketch point to current point in loop
Set sketchPoint = vpoint
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code we set **SOLIDWORKS Sketch point** variable `sketchPoint` to current point `vpoint` in loop

{% capture code_content %}

```vb
' Create Counter Bore Hole at this point
Set swHoleFeature = swDoc.FeatureManager.HoleWizard(swWzdCounterBore, swStandardISO, swStandardISOHexCapScrew, "M6", swEndCondBlind, 5 * LengthConversionFactor, 6 * LengthConversionFactor, 10 * LengthConversionFactor, 5 * LengthConversionFactor, 0, 1 * LengthConversionFactor, 2 * LengthConversionFactor, 0, 0, 0, 0, 0, 0, 0)
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code we set the value of variable `swHoleFeature` by `HoleWizard` method.

This `HoleWizard` method takes following parameters as explained:

  - **GenericHoleType** - *Type of hole or slot as defined in `swWzdGeneralHoleTypes_e` as given in below table*

{% include fluent-table.html
  caption="General hole types"
  headers="Member|Description"
  rows="
`swWzdCounterBore`|**0**
`swWzdCounterBoreSlot`|**6**
`swWzdCounterSink`|**1**
`swWzdCounterSinkSlot`|**7**
`swWzdHole`|**2**
`swWzdHoleSlot`|**8**
`swWzdLegacy`|**5**
`swWzdPipeTap`|**3**
`swWzdTap`|**4**
"
%}

  - **StandardIndex** - *Hole or slot standard property as defined in `swWzdHoleStandards_e` as given in below table*.

{% include fluent-table.html
  caption="Hole standards"
  headers="Member|Description"
  rows="
`swStandardAnsiInch`|**0**
`swStandardAnsiMetric`|**1**
`swStandardAS`|**16 = Australian**
`swStandardBSI`|**2**
`swStandardDIN`|**4**
`swStandardDME`|**3**
`swStandardGB`|**13**
`swStandardHascoMetric`|**5**
`swStandardHelicoilInch`|**6**
`swStandardHelicoilMetric`|**7**
`swStandardIS`|**15 = Indian**
`swStandardISO`|**8**
`swStandardJIS`|**9**
`swStandardKS`|**14 = Korean**
`swStandardPCS`|**10**
`swStandardPEMInch`|**17 = PEM Inch**
`swStandardPEMMetric`|**18 = PEM Metric**
`swStandardProgressive`|**11**
`swStandardSuperior`|**12**
"
%}

  - **FastenerTypeIndex** - *Hole or slot fastener type as defined in `swWzdHoleStandardFastenerTypes_e` as given in 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/swconst/solidworks.interop.swconst~solidworks.interop.swconst.swwzdholestandardfastenertypes_e.html" text="**this page of Solidworks API Help**" %}.

  - **SSize** - *Size of the hole or slot.*

  - **EndType** - *Hole or slot end type as defined in `swEndConditions_e` as given in below table*.

{% include fluent-table.html
  caption="End conditions"
  headers="Member|Description"
  rows="
`swEndCondBlind`|**0**
`swEndCondMidPlane`|**6**
`swEndCondOffsetFromSurface`|**5**
`swEndCondThroughAll`|**1**
`swEndCondThroughAllBoth`|**9**
`swEndCondThroughNext`|**2**
`swEndCondUpToBody`|**7**
`swEndCondUpToNext`|**11**
`swEndCondUpToSelection`|**10**
`swEndCondUpToSurface`|**4 = Do not use; superseded by `swEndCondUpToSelection`**
`swEndCondUpToVertex`|**3 = Do not use; superseded by `swEndCondUpToSelection`**
"
%}

  - **Diameter** - *Diameter of the hole or slot*.

  - **Depth** - *Depth of the hole or slot*.

  - **Value1** - *Hole or slot parameter*.

  - **Value2** - *Hole or slot parameter*.

  - **Value3** - *Hole or slot parameter*.

  - **Value4** - *Hole or slot parameter*.

  - **Value5** - *Hole or slot parameter*.

  - **Value6** - *Hole or slot parameter*.

  - **Value7** - *Hole or slot parameter*.

  - **Value8** - *Hole or slot parameter*.

  - **Value9** - *Hole or slot parameter*.

  - **Value10** - *Hole or slot parameter*.

  - **Value11** - *Hole or slot parameter*.

  - **Value12** - *Hole or slot parameter*.

**Return Value** : This `HoleWizard` method return **feature** data object.

To see methods and properties related to `Feature` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.IFeature_members.html" text="**this page of SOLIDWORKS API Help**" %}.

In our code, I have used following values:

{% include fluent-table.html
  caption="HoleWizard values used"
  headers="Parameter Name|Value Used"
  rows="
**GenericHoleType**|`swWzdCounterBore`
**StandardIndex**|`swStandardISO`
**FastenerTypeIndex**|`swStandardISOHexCapScrew`
**SSize**|`M6`
**EndType**|`swEndCondBlind`
**Diameter**|`5 * LengthConversionFactor`
**Depth**|`6 * LengthConversionFactor`
**Value1**|`10 * LengthConversionFactor`
**Value2**|`5 * LengthConversionFactor`
**Value3**|`0`
**Value4**|`1 * LengthConversionFactor`
**Value5**|`2 * LengthConversionFactor`
**Value6**|`0`
**Value7**|`0`
**Value8**|`0`
**Value9**|`0`
**Value10**|`0`
**Value11**|`0`
**Value12**|`0`
"
%}

`HoleWizard` method is part of `FeatureManager` object.

This `FeatureManager` is again part of `swDoc` variable i.e. `ModelDoc2` object.

To see methods and properties related to `FeatureManager` object, please visit 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/SolidWorks.Interop.sldworks~SolidWorks.Interop.sldworks.IFeatureManager_members.html" text="**this page of SOLIDWORKS API Help**" %}.

Please see below image in which I have identity some **`value`** parameters.

{% include image-zoom.html
  src="/assets/Solidworks_Images/hole-wizard/hole-wizard-parameters.png"
  alt="Hole wizard parameter values"
  caption="Figure: Hole wizard parameter values"
%}

{% capture code_content %}

```vb
' Check if Holes from Hole Wizard creates or not
If swFeature Is Nothing Then
  MsgBox ("Failed to create Holes from Hole Wizard.")
  Exit Sub
End If
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line of code, we use an 👉 {% include link-new-tab.html href="/vba/if-then-structure-select-case/" text="**IF statement**" %} to check if we able to create **Holes from 👉 {% include link-new-tab.html href="https://help.solidworks.com/2019/english/api/sldworksapi/solidworks.interop.sldworks~solidworks.interop.sldworks.ifeaturemanager~holewizard.html" text="Hole Wizard" %}** Feature or not.

If we failed to select then inform the user by a 👉 {% include link-new-tab.html href="/vba/msgBox-function/" text="**Message Window**" %}.

After showing error message our program **exit** from here itself.

Now we run the macro and after running macro we get Revolve as shown in below image.

{% include image-zoom.html
  src="/assets/Solidworks_Images/hole-wizard/hole-wizard-final-result.png"
  alt="Hole wizard final result"
  caption="Figure: Hole wizard final result"
%}

### Final work

After creating **Hole Cut** feature, we have to do some cleaning work so that we can use this macro frequently.

* Make part Zoom to fit

{% capture code_content %}

```vb
' View zoom to fit
swDoc.ViewZoomtofit2
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line, we make our view zoom to fit the model.

For this we use `ViewZoomtofit2` method which is part of *SOLIDWORKS Document* variable i.e `swDoc` variable.

* Clear selection

{% capture code_content %}

```vb
' Clear all selection
swDoc.ClearSelection2 True
```

{% endcapture %}

{% include code-block.html
  content=code_content
  lines="true"
%}
In above line, we clear all previous selection.

For this we use `ClearSelection2` method which is part of *SOLIDWORKS Document* variable i.e `swDoc` variable.

**This is it !!!**

*I hope my efforts will helpful to someone!*

If you found anything to **add or update**, please let me know on my *e-mail*.

Hope this post helps you to **create Hole Feature** with SOLIDWORKS VBA Macros.

For more such tutorials on **SOLIDWORKS VBA Macro**, do come to this blog after sometime.

*If you like the post then please share it with your friends also.*

*Do let me know by you like this post or not!*

*Till then, Happy learning!!!*

- {% include link-new-tab.html
  href="/download-solidworks-macros/"
  text="🎁 Download 5 Free SolidWorks Macros →"
%}

</div>
